version: '3.8'

services:
  web:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "7777:7777"
    volumes:
      - ./app:/app
      - ./database:/database
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    container_name: househunter-app
